This directory contains the utilities for handling importing and exporting dictionary data.

It is split up into versions to handle different versions of the exported dictionary schema.

The code will need to detect the schema version and then handle it appropriately for backwards
compatibility.
