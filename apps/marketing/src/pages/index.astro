---
const locale = "en";
---

<BaseLayout locale={locale}>
  <AnimatedBackground client:only="react" transition:persist />
  <Hero locale={locale} />
  <Features locale={locale} />
  <HowItWorks locale={locale} />
  <!-- Commented out until I get actual stats -->
  <!-- <Stats locale={locale} /> -->
  <CTA locale={locale} />
</BaseLayout>
